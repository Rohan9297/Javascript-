// javascript is the developed by the bradman
// in the 
var name=prompt("enter your name");
var first_character=name.slice(0,1);
alert("Hello,"+first_character.toUpperCase()+name.slice(1,name.length).toLowerCase());
